# QuickFace 
# Software Engineering Course Project(Autumn 2016)
# G26 - Bo Pang, Weiming Bao & Zifan Liu
# v 1.0.0 - 2017.1.6


# License: Apache License 2.0


# File Structure

{ROOT_DIR}
{ROOT_DIR}/QuickFace		Source Code
{ROOT_DIR}/build 			MakeFile and .o
{ROOT_DIR}/install 			Installation Directory


# Installation Guide

1. Delete all the files under 'build' folder except 'face_landmarks_model_rcr_68.bin'.
2. Open a terminal in folder 'build'.
3. Type 'cmake -DCMAKE_INSTALL_PREFIX=../install -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=gcc-5 -DCMAKE_CXX_COMPILER=g++-5 -DOpenCV_haarcascades_DIR=/usr/share/opencv/haarcascades/ ../QuickFace/'.
4. Type 'make install'.
5. The server-end application has been installed to {ROOT_DIR}/install/bin.


# Requiring Packages:

boost (>=1.54.0)
OpenCV (>=2.4.8)
Eigen (>=3.2.0)
a recent compiler (>=gcc-4.9)
ubuntu (>=14.04) - optional

# 'apt-get' Instructions for Requiring Packages:

sudo add-apt-repository ppa:ubuntu-toolchain-r/test
sudo apt-get update
sudo apt-get install gcc-5 g++-5 libboost-all-dev libeigen3-dev libopencv-dev opencv-data